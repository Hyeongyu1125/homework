import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css"; // Tailwind 또는 일반 CSS

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
